View(largest_hispanic)
#replace unclean dataname for Montgomery County
hispanic_pop_clean <- maryland_hispanic %>%
mutate(clean_hispanic_pop = case_when(
name == "Montgomery County,Maryland" ~ "Montgomery County, Maryland",
TRUE ~ as.character(name)
))
updated_hispanic <- hispanic_pop_clean %>%
inner_join(maryland_pop, by="name")
largest_hispanic <- updated_hispanic %>%
mutate(hisp_pct = percent(hispanic_pop / total_pop) * 100) %>%
arrange(desc(hisp_pct))
#Montgomery County
#replace unclean dataname for Montgomery County
hispanic_pop_clean <- maryland_hispanic %>%
mutate(clean_hispanic_pop = case_when(
name == "Montgomery County,Maryland" ~ "Montgomery County, Maryland",
TRUE ~ as.character(name)
)) %>%
select(clean_hispanic_pop, hispanic_pop)
updated_hispanic <- hispanic_pop_clean %>%
inner_join(maryland_pop, by="name") %>%
select(clean_hispanic_pop, hispanic_pop, total_pop)
#replace unclean dataname for Montgomery County
hispanic_pop_clean <- maryland_hispanic %>%
mutate(clean_hispanic_pop = case_when(
name == "Montgomery County,Maryland" ~ "Montgomery County, Maryland",
TRUE ~ as.character(name)
)) %>%
select(name, clean_hispanic_pop, hispanic_pop)
updated_hispanic <- hispanic_pop_clean %>%
inner_join(maryland_pop, by="name") %>%
select(clean_hispanic_pop, hispanic_pop, total_pop)
largest_hispanic <- updated_hispanic %>%
mutate(hisp_pct = percent(hispanic_pop / total_pop)) %>%
arrange(desc(hisp_pct))
#Montgomery County
View(largest_hispanic)
#replace incorrect Somerset value
white_md_clean <- maryland_white %>%
mutate(clean_white_pop = case_when(name == "Somerset County, Maryland" & geoid == 24039 ~ 13510, TRUE ~ white_pop))
sum(white_md_clean$clean_white_pop)
#3,275,048 total population of people who identify as white alone in Maryland
black_md_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop))
mean(maryland_black_clean$clean_black_pop)
black_md_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop))
mean(black_md_clean$clean_black_pop)
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop))
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(clean_black_pop)
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
#select(clean_black_pop, )
mean(maryland_black_clean$clean_black_pop)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
mean(maryland_black_clean$clean_black_pop)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(name, clean_black_pop)  # Selecting only the columns you want
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
))
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(name, clean_black_pop, geoid)
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(geoid, name, clean_black_pop)
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore City, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(geoid, name, clean_black_pop)
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(geoid, name, clean_black_pop)
mean(maryland_black_clean$clean_black_pop)
View(maryland_black_clean)
#replace N/As with data
maryland_black_clean <- maryland_black %>%
mutate(clean_black_pop = case_when(
name == "Baltimore city, Maryland" & geoid == 24510 ~ 358453,
name == "Baltimore County, Maryland" & geoid == 24005 ~ 272596,
TRUE ~ black_pop
)) %>%
select(geoid, name, clean_black_pop)
mean(maryland_black_clean$clean_black_pop)
#Average Black population for all Maryland counties is 75,670 people
setwd("~/Documents/GitHub/olivia_borgula_jour472/reverse_engineering")
#upload data
fatal_shootings <- read_csv("data/fatal-police-shootings.csv")%>%
clean_names()
agencies <- read_csv("data/fatal-police-shootings-agencies.csv")%>%
clean_names()
View(agencies)
View(fatal_shootings)
View(Action)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
#add up total number of women shot
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
fatal_shootings%>%
select(gender)%>%
filter(gender == women)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == women)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == female)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == "female")
#add up total number of women shot
View(women_shootings)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == "female")%>%
count_tracts = n()
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == "female")%>%
nrow(women_shootings)
#filter out data past september 4, 2020
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- fatal_shootings%>%
filter(gender == "female")
nrow(women_shootings)
#add up total number of women shot
#filter out data past september 4, 2020
#changing the format of date column into a new column
shootings_clean <- data.frame(date = c("2015-01-02", "2015-02-02", "2015-03-02"))
#Convert date column to Date object
shootings_clean <- shootings %>%
mutate(date = as.Date(date))
#filter out data past september 4, 2020
#changing the format of date column into a new column
shootings_clean <- data.frame(date = c("2015-01-02", "2015-02-02", "2015-03-02"))
#Convert date column to Date object
shootings_clean <- fatal_shootings %>%
mutate(date = as.Date(date))
# Change date format to a different one
shootings_clean <- shootings_clean %>%
mutate(year = format(date, "%Y"))
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- shootings_clean%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
View(fatal_shootings)
View(shootings_clean)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-09-04"))
View(filtered_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-09-04"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootingsn%>%
filter(gender == "female")
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-09-04"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-07-03"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-09-01"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-31"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-04"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-07-04"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-31"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-30"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
View(shootings_clean_x2015_x2020)
#Sample data frame with a date column
#shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
#shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
#updated_shootings <- fatal_shootings%>%
#mutate(date = as.Date(date))
# Filter data for the specified date range
fatal_shootings%>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
View(fatal_shootings)
#Sample data frame with a date column
#shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
#updated_shootings <- fatal_shootings%>%
#mutate(date = as.Date(date))
# Filter data for the specified date range
fatal_shootings%>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(fatal_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
View(fatal_shootings)
#Sample data frame with a date column
#shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#Sample data frame with a date column
shootings_clean_x2015_x2020 <- data.frame(date = c("2022-01-15", "2022-02-20", "2022-03-25", "2019-05-10", "2020-08-30", "2015-12-20"))
#Convert date column to Date object
updated_shootings <- fatal_shootings%>%
mutate(date = as.Date(date))
# Filter data for the specified date range
filtered_shootings <- updated_shootings %>%
filter(date <= as.Date("2020-08-29"))
#add up the total number of people shot
nrow(filtered_shootings)
#filter for women
women_shootings <- filtered_shootings%>%
filter(gender == "female")
#add up total number of women shot
nrow(women_shootings)
#filter by race and whether they were armed
women_by_race <- filtered_shootings%>%
select(race, armed_with)
filter(race == "B")
#filter by race and whether they were armed
women_by_race <- filtered_shootings%>%
select(race, armed_with)
filter(race == "B")
#filter by race and whether they were armed
women_by_race <- filtered_shootings%>%
select(race, armed_with)%>%
filter(race == "B")
#add up total number of women shot
nrow(women_by_race)
#filter by race and whether they were armed
women_by_race <- filtered_shootings%>%
filter(race == "B")
#add up total number of women shot
nrow(women_by_race)
#filter by race and whether they were armed
women_by_race <- women_shootings%>%
filter(race == "B")
#add up total number of women shot
nrow(women_by_race)
View(women_by_race)
#filter by race and whether they were armed
black_women <- women_shootings%>%
filter(race == "B")
#add up total number of women shot
nrow(black_women)
#filter by unarmed
black_women_unarmed <- black_women%>%
filter(armed_with == "unarmed")
#add up number
nrow(black_women_unarmed)
